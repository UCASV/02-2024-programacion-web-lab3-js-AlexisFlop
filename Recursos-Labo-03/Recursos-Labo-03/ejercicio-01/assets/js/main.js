const countVotes = (votes) => {
    const votesCounted = [];
    votes.forEach(candidate => {
        
        
    });
}

const showResults = (/*recibe*/) => {
    //Code
}

const main = () => {
    let votes = [
        { candidate: 'Alice' },
        { candidate: 'Bob' },
        { candidate: 'Alice' },
        { candidate: 'Alice' },
        { candidate: 'Bob' }
    ];
    

}

main();
